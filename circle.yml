machine:
  python:
    version: 3.5.1
  environment:
    BANK: '{"dev":true}'

dependencies:
  pre:
  - pip install django_nose
deployment:
  staging:
    branch: dev
    heroku:
      appname: banking-stage
    commands:
      - npm i --only=dev
      - BANK='{"dev":false}' webpack
