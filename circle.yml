machine:
  python:
    version: 3.5.1
  environment:
    BANK: '{"db":"sqlite"}' # use qlite for tests

dependencies:
  pre:
    - pip install django_nose
    - npm i --only=dev
    - webpack

deployment:
  staging:
    branch: dev
    heroku:
      appname: banking-stage
